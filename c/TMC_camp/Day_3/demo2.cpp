#include <stdio.h>

int main(int argc, char const *argv[])
{
	for (int i = 1; i <= 2020; i++)
	{
		printf(", %d", i*i);
	}
	return 0;
}









if ((pow(i,1.0/3)*pow(i,1.0/3)*pow(i,1.0/3)) == (i*i*i))
			{
				printf(">> %d\n", i);
			}